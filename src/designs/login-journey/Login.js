import React from "react";
const Login = (props) => {
    return (
        <div>
                                                            <h1>Login Page {props.test}</h1>
                    </div>
    )
}

export default Login
