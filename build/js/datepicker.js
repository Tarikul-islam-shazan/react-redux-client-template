$(document).ready(function(){
		// $(".datepicker").datepicker({
		// 	autoclose: true,
		// 	todayHighlight: true,
		// 	format: 'dd/mm/yyyy'
		// }).datepicker('update', new Date());
		// $(".datepicker").datepicker({
		// 	autoclose: true,
		// 	todayHighlight: true,
		// 	format: 'dd/mm/yyyy'
		// }).on('changeDate', function (e) {
	  //   	// console.log("changeDate",e.format())
		// 		$('.custom-way').val(e.format())
		// 		$( ".custom-way" ).trigger( "change" );
		// });
});

// Toggle Responsive Menu
